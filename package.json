{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "prepare": "",
    "start": "pnpm --filter oralcare-frontend run start"
  },
  "private": true,
  "devDependencies": {
    "@types/node": "catalog:",
    "husky": "^9.1.7",
    "orval": "^7.5.0",
    "prettier": "^3.8.1",
    "typescript": "~5.9.2",
    "vitest": "^4.1.7"
  },
  "dependencies": {
    "esbuild": "0.27.3"
  }
}